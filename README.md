# ToDo App

This contains the code for a todo api.

## Infrastructure

The code builds a google cloudrun image based on a minimal nixos image, that
allows to access to user's todos.

## Tasks

- [x] Generate a skeleton google cloud run code in golang.
- [ ] Generate a podman image for google cloud run
- [ ] Generate terraform code that deploys that image
